name: NCOMAI
options:
  bundleIdPrefix: com.pfn000
  deploymentTarget:
    iOS: '17.0'
settings:
  base:
    SWIFT_VERSION: '5.0'
    IPHONEOS_DEPLOYMENT_TARGET: '17.0'
targets:
  NCOMAI:
    type: application
    platform: iOS
    sources:
      - path: NCOMApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.pfn000.NCOMAI
        INFOPLIST_KEY_CFBundleDisplayName: NCOM AI
        INFOPLIST_KEY_NSLocalNetworkUsageDescription: NCOM uses your local network to connect to your NCOM runtime.
        TARGETED_DEVICE_FAMILY: '1,2'
        GENERATE_INFOPLIST_FILE: YES
    scheme:
      testTargets: []
